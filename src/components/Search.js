var React = require("react");

class Search extends React.Component {
    render() {
        var result = null;

        if (this.props.result === "search") {
            
        }

        return (
            <div>
                {result}
            </div>
        );
    }
}

module.exports = Search;